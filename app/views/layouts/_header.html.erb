<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "Members Only", login_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <%# if logged_in? %>
        <% if false %>
          <%= link_to "Log out", logout_path, method: :delete %>
        <% else %>
          <li><%= link_to "Log in", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>
